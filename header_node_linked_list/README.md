# Project's struct
- `header_node.h`: Definition of the data type stored in the list, the pointer "list" and function prototypes.
- `header_node.c`: Definition of the data types "list" and "header", implementation of the functions.
	- `main.c`: Management of the user interface and input/output (I/O) operations.
- `header_node_test.c`: Implementation of the test functions.
	- `main_test.c`: Unitary test for validate the creation and manipulation.

	The purpose of this project is to learn the applications of linked list with header node.
	
• Linked list with header node: A linked list variation in which uses a special node called "header" to store some informations of the list.
