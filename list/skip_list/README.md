# Project's struct
- `sequential_list.h`: Definition of the data type stored in the list, the pointer "sl" and function prototypes.
- `sequential_list.c`: Definition of the data types "sl", implementation of the functions.
- `sequential_list_test.c`: Unitary test for validate the creation and manipulation.

	The purpose of this project is to learn the applications of skip list.
	
• Skip list: Your implementation utilises a struct similar to a linked dynamic list. Each node has an array of levels. It's the array that links between the differentes list's levels

• Disadvantages: The maximum level's number of the skip list needs to be previously defined. This limits the number of leves that can be
